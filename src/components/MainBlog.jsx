import React from 'react';
import SectionBgImage from './SectionBgImage'
import SectionAuthor from './SectionAuthor'
import SectionContentBlog from './SectionContentBlog'

const MainBlog = ({children}) => (
  <main>
    {children}
  </main>
);

export default MainBlog;
